<template name="icon">
  <view style="{{style||''}}" class="icon" data-side="{{side||''}}" data-name="{{name}}" bindtap="onIconClick">
    <image class="icon-img {{size||'normal'}}" src="../../images/{{name}}.png" mode="{{mode||'aspectFit'}}"></image>
  </view>
</template>